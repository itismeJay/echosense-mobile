# Copy to .env.local for LAN development. This file contains no secrets.
EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.92:8000

# Preview and production EAS profiles use HTTPS values from eas.json.
# Production must never use a LAN address or plain HTTP.
